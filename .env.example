# ---- AI Health Assistant ----
# Set ONE of the following (the API route auto-detects which is present).
# Without a key, the chatbot still works using its built-in keyword fallback.

# OpenAI (uses gpt-4o-mini)
OPENAI_API_KEY=

# Anthropic (uses claude-3-5-haiku) — takes priority if both are set
ANTHROPIC_API_KEY=
